# dgloss-lp

AIテレアポ 無料デモ体験 ｜ DGLOSS のランディングページ（静的HTML）。

- 本番: GitHub Pages（`main` ブランチ / ルート）
- 実体: `index.html`（自己完結・画像はbase64、外部依存はGoogle Fontsのみ）

## 更新方法
`index.html` を編集して `main` に push すると自動反映されます。
